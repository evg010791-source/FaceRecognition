<Window x:Class="FaceRecognition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FaceRecognition"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Отметить как распознанного"/>
            <MenuItem Header="Отметить как нераспознанного"/>
        </ContextMenu>
    </Window.ContextMenu>
    <DockPanel LastChildFill="False">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Настройки"/>
                <Separator/>
                <MenuItem Header="Выход"/>
            </MenuItem>
            <MenuItem Header="Вид">
                <MenuItem Header="Обновить"/>
            </MenuItem>
            <MenuItem Header="Справка">
                <MenuItem Header="О программе"/>
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="Запуск камеры"/>
                <Button Content="Остановить"/>
                <Separator/>
                <Button Content="Диагностика"/>
                <Button Content="Настройки"/>
                <Button Content="Обновить"/>

            </ToolBar>
        </ToolBarTray>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Список студентов" FontWeight="Bold" Margin="5" TextAlignment="Center" />
                    <ListBox>
                        <ListBoxItem>Иванов Иван (1ПБ25Н)</ListBoxItem>
                        <ListBoxItem>Иванов Иван (1ПБ25Н)</ListBoxItem>
                        <ListBoxItem>Иванов Иван (1ПБ25Н)</ListBoxItem>
                        <ListBoxItem>Иванов Иван (1ПБ25Н)</ListBoxItem>
                    </ListBox>

                </DockPanel>
            </Border>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5"/>
            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" Margin="5" Width="500">
                <Grid Background="LightGray">
                    <TextBlock Text="Область Видео" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="DarkGray" FontSize="20"/>
                </Grid>
            </Border>
        </Grid>
        
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="Готов к работе"/>
            </StatusBarItem>
        </StatusBar>
    </DockPanel>
    
</Window>
